
import React from 'react';

const AgentOverview = () => {
    return (
        <div>
            <h2>Agent Overview</h2>
            <p>List all deployed agents with their current status (active, idle, error).</p>
            <p>Include metrics like the number of transactions executed, success rate, and performance over time.</p>
        </div>
    );
};

export default AgentOverview;
